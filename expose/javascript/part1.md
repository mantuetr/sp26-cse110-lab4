1. values added:  20
2. final result:  20
3. Because it's known as function scope. So, in a function, you can access
   to the variables anywhere, causing naming conflicts or scoping issues
4. values added:  20
5. Error. Because "let" is a block-scoped so cannot use outside the if block.  
   So, the code cannot define "result". And, return error
6. It causes error, because when using "const", you cannot change the value 
   after that so to assign "result = num1 + num2" will cause an error before
   line 9 to print
7. It causes error, because when using "const", you cannot change the value 
   after that so to assign "result = num1 + num2" will cause an error before
   line 13 to print
