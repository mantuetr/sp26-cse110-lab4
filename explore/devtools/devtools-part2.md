1. The code reads num1 and num2 as strings, so they concatenate two together, 
   not add it
2. I changed the typeof num1 and num2 to Number, so now the code should read it
   as integers, and they can be added together now. [fix](fix.png)
